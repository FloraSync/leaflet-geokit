# Copy this file to .env and adjust values as needed.

# --- Git identity ---
PUBLISHER_GIT_NAME=
PUBLISHER_GIT_EMAIL=

# --- GitHub origin and branch ---
# HTTPS repo URL (will be offered as default and can be reset explicitly)
PUBLISHER_ORIGIN_HTTPS=https://github.com/FloraSync/leaflet-geokit.git
PUBLISHER_BRANCH=main

# --- GitHub authentication ---
# g=use GitHub CLI (device/web flow), p=use PAT, n=skip
PUBLISHER_GH_AUTH=g
# If using PAT
GITHUB_USERNAME=
GITHUB_TOKEN=

# --- npm publish ---
# Recommended: set an npm token for publish (https://www.npmjs.com/settings/<you>/tokens)
NPM_TOKEN=
# Auto-configure npm token without prompt (uses NPM_TOKEN)
PUBLISHER_CONFIGURE_NPM=

# --- package.json quick update (optional) ---
PUBLISHER_PACKAGE_NAME=@florasync/leaflet-geokit
PUBLISHER_PACKAGE_AUTHOR=FloraSync
PUBLISHER_UPDATE_PKG=

# --- Automation toggles (non-interactive) ---
# Skip typecheck + unit tests during container run
PUBLISHER_SKIP_TESTS=
# Auto-publish to npm (yes/no/true/1)
PUBLISH=
# Auto-push to GitHub (yes/no/true/1)
PUSH=
# Pre-set commit message when pushing (optional)
PUBLISHER_COMMIT_MSG=
# Bypass husky hooks on push (yes/no/true/1)
PUBLISHER_BYPASS_HUSKY=

# --- Devcontainer helpers ---
# Install @openai/codex CLI globally inside the devcontainer
DEVCONTAINER_INSTALL_CODEX=
# OPENAI_API_KEY will be exported in ~/.bashrc for dev shells
OPENAI_API_KEY=
# GEMINI_API_KEY will be exported in ~/.bashrc for dev shells
GEMINI_API_KEY=
